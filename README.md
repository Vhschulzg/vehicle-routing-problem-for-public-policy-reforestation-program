# vehicle-routing-problem-for-public-policy-reforestation-program
A truck and trailer routing problem solved by using 3 steps involving clustering  (k-means) of a large number of locations, then time matrices are created using real-world times from the googlemaps API so an approximate algorithm can run on them, and finally mixed-integer linear programming for allocation of workloads.
